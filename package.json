{
  "name": "pixl-tweak",
  "version": "2.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && npx serve . --listen 3000",
    "watch": "node --watch-path=. build.mjs"
  },
  "devDependencies": {
    "@jsquash/jpeg": "^1.6.0",
    "@jsquash/oxipng": "^2.3.0",
    "esbuild": "^0.28.1"
  }
}
