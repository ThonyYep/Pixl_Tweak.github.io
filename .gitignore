# Claude Code local settings and preview config
.claude/

node_modules/

# NOT ignored on purpose: bundle.js and selftest.bundle.js. GitHub Pages serves
# this repository as-is and runs no build, so the built files have to be here.

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# NOT ignored on purpose: vendor/. build.mjs copies the WASM codecs out of
# node_modules, and GitHub Pages has to serve them.
